From d98fec36bd6b7a6b6e4287c00106618cb42a1e44 Mon Sep 17 00:00:00 2001
From: SQLite-Enhance Builder <build@sqlite-enhance.local>
Date: Mon, 9 Mar 2026 15:09:36 +0000
Subject: [PATCH] Stage 4: Add SIMD optimizations

---
 sqlite3.c | 10 ++++++++++
 1 file changed, 10 insertions(+)

diff --git a/sqlite3.c b/sqlite3.c
index 1f2413b..d31f354 100644
--- a/sqlite3.c
+++ b/sqlite3.c
@@ -58628,8 +58628,16 @@ static void pagerUnlockAndRollback(Pager *pPager){
 ** correct and the middle be corrupt.  Thus, this "checksum" scheme,
 ** though fast and simple, catches the mostly likely kind of corruption.
 */
+
+/* SQLite-Enhance: SIMD 优化 */
+#include "enhance/simd_ops.h"
+
 static u32 pager_cksum(Pager *pPager, const u8 *aData){
   u32 cksum = pPager->cksumInit;         /* Checksum value to return */
+  CPUFeatures f = detect_cpu_features();
+  if (f.has_avx2) {
+    return cksum + simd_checksum(aData, pPager->pageSize);
+  }
   int i = pPager->pageSize-200;          /* Loop counter */
   while( i>0 ){
     cksum += aData[i];
@@ -58637,6 +58645,8 @@ static u32 pager_cksum(Pager *pPager, const u8 *aData){
   }
   return cksum;
 }
+/*
+*/

 /*
 ** Read a single page from either the journal file (if isMainJrnl==1) or
--
2.52.0
